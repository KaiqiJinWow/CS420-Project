这是我们的在论文中用来对比的baseline 对比模型，用caffe训练的,相对比较简单。他的表现大约为45%。
train_val是我们的训练网络，solver是我们的模型训练设置，developer是我们的测试用的网络。由于小组成员本地没有caffe环境，使用的是云端训练，所以具体的训练出来的model就暂时无法提供了。如果有需要的话可以联系jjyy8763@163.com提供
在使用的时候记得将train_val中的mean和data的路径设置成自己mean和data的路径。其余只需要按照caffe基本操作来开始训练就可以了。需要注意的是caffe只能用lmdb来训，所以需要预先转一下lmdb格式。具体可以参见caffe根目录下的create_iamgenet.sh脚本，修改其中的一些参数设置就可以直接用来将我们的png转化成lmdb格式了。
