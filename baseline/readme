这是我们的baseline 对比模型，用caffe训练的,他的表现大约为45%。在使用的时候记得将train_val的路径设置成自己的路径。其余只需要按照caffe基本操作来开始训练就可以了。需要注意的是caffe只能用lmdb来训，所以需要预先转一下lmdb格式。具体可以参见caffe根目录下的create_iamgenet.sh脚本，修改其中的一些参数设置就可以直接用来将我们的png转化成lmdb格式了。
